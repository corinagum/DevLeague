extends ./../layout

block content
  ul
    each item in products
      li Product Name:&nbsp
        a(href="/products/#{item.id}") #{item.name}&nbsp
        a(href="/products/#{item.id}/edit") edit&nbsp
        a() delete


